# Database
DATABASE_URL=postgresql+asyncpg://mentorled:mentorled_dev@localhost:5432/mentorled

# Redis
REDIS_URL=redis://localhost:6379

# Anthropic API - GET YOUR KEY FROM: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-xxxx

# Auth - Generate with: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-in-production-use-openssl-rand-hex-32

# CORS - Allow requests from frontend (all possible Next.js ports)
CORS_ORIGINS=["http://localhost:3000","http://localhost:3001","http://localhost:3002"]

# Agent Settings
SCREENING_CONFIDENCE_THRESHOLD=0.7
RISK_ALERT_THRESHOLD=0.5

# Cost Tracking (USD per 1M tokens)
COST_PER_1M_INPUT_TOKENS=3.0
COST_PER_1M_OUTPUT_TOKENS=15.0
